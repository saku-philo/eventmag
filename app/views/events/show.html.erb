<div class="container">
  <h3>イベント詳細</h3>
  <div class="card shadow img-thumbnail mx-auto" style="max-width: 50rem;">
    <!-- イベント画像表示部分 -->
    <% if @event.image.url.presence %>
      <%= image_tag @event.image.url, class: 'img-fluid' %>
    <% else %>
      <!-- 'no_image.png'は、assets/images内に配置 -->
      <%= image_tag 'no_image.png', class: 'img-fluid', style: 'border: 1px solid lightgray' %>
    <% end %>
    <div class="card-body">
      <strong><p>投稿者：<%= @event.user.name %></p></strong>
      <p>イベント名：<%= @event.name %></p>
      <p>日時：<%= @event.date %></p>
      <p>開催場所：<%= @event.place %></p>
      <p>開始時間：<%= "#{@event.start_at.hour}:#{@event.start_at.min}" %></p>
      <p>終了時間：<%= @event.end_at %></p>
      <p>参加費用：<%= "#{@event.cost}円" %></p>
      <p>url：<%= @event.link_url %></p>
      <p>備考：<%= @event.meta %></p>
      <br/>
      <p>現在の参加者数：<%=@guests.count %>人</p>
      <% @guests.each do |g| %>
        <p><%= g.name %>さん</p>
      <% end %>
      <%= link_to '一覧へ', events_path, class: 'btn btn-primary' %>
      <%= link_to '編集', edit_event_path(@event), class: 'btn btn-success' %>
      <%= link_to '削除', event_path(@event),
                          method: :delete,
                          data: { confirm: "タスク「#{@event.name}」を削除します。よろしいですか？" },
                          class: 'btn btn-danger' %>

      <!-- イベントへの参加（attend create）ボタン -->
      <!-- ログインしていて、イベントに参加申し込みをしている場合、キャンセルボタンを表示する -->
      <% if current_user && user_apply_event?(current_user,@event)  %>
        <%= link_to '参加キャンセル', attend_path(@attend),
                                          method: :delete, class: 'btn btn-danger' %>
      <% else %>
        <!-- ログインしていない場合、ログイン画面に遷移させる -->
        <%= link_to '参加する', attends_path(event_id: @event.id),
                                          method: :post, class: 'btn btn-primary' %>
      <% end %>
      <%= link_to 'お問い合わせ', new_contact_path(event_id: @event.id),
                                                  class: 'btn btn-info' if current_user %>
    </div>
  </div>
</div>